---
type: handover
tags: [deploy, vercel, tin-tuc, layout, bugfix, trang-chu]
status: completed
date: 2026-05-09
---

# HANDOVER — Phiên 2026-05-09 (001)

> Deploy Vercel, Fix build errors, Bố trí lại tin tức trang chủ 7:3

---

## 1. SNAPSHOT

| Thông tin | Chi tiết |
|-----------|----------|
| **Giai đoạn** | Deploy demo + Nâng cấp giao diện tin tức trang chủ |
| **Task hoàn thành** | 5/5 |
| **Task dở dang** | Không |
| **Tổng pages** | 38 (build thành công) |
| **Vercel deploy** | Đã push code, đang chờ/deploy trên Vercel |

### File đã tạo mới

| File | Mô tả |
|------|-------|
| `src/components/NewsTabs.tsx` | Client component — tabs lọc chuyên mục + thanh tìm kiếm |
| `src/components/NewsSection.tsx` | Server component — section tin tức trang chủ layout 7:3 |
| `src/app/(public)/tin-tuc/NewsPageClient.tsx` | Client component — trang tin tức (tabs + search + sidebar) |

### File đã sửa

| File | Thay đổi |
|------|----------|
| `src/app/(public)/page.tsx` | Thay inline news → `<NewsSection />` component |
| `src/app/(public)/tin-tuc/page.tsx` | Tách Server/Client, import `NewsPageClient` |
| `src/app/globals.css` | Thêm `.scrollbar-hide` cho tabs mobile |
| `package.json` | `build: "prisma generate && next build"` |
| `prisma.config.ts` | Fallback URL khi chưa có DATABASE_URL |
| `src/lib/db.ts` | Trả null thay vì throw khi chưa có DB |
| `src/lib/queries.ts` | `getPrisma()` với null check |
| `prisma/schema.prisma` | Bỏ deprecated `driverAdapters` preview feature |
| `.gitignore` | `/useful-links.json` (chỉ root), thêm `facebook-xuan-hoa-final/`, `knowledge/` |

---

## 2. ACHIEVEMENTS

### Nghiên cứu & nâng cấp tin tức ✅
- Phân tích bố cục **hochiminhcity.gov.vn** + **ubmttq.hochiminhcity.gov.vn**
- Trang chủ: Layout **7:3** — Tin nổi bật + 3 tin nhỏ (trái) | Banner tuyên truyền + Tổng đài 1022 (phải)
- Trang /tin-tuc: **Tabs lọc** chuyên mục + **Tìm kiếm** realtime + **Sidebar** tin mới

### Chuẩn bị deploy Vercel ✅
- Commit & push toàn bộ code lên GitHub (178 files)
- Hướng dẫn deploy chi tiết (Environment Variables, checklist)

### Fix build errors Vercel ✅
- **Lỗi 1**: `PrismaClient` import fail — fix bằng `prisma generate` trong build script + fallback URL
- **Lỗi 2**: `prisma` possibly null — fix bằng `getPrisma()` helper
- **Lỗi 3**: `useful-links.json` ENOENT — fix `.gitignore` chỉ ignore root file

### Bố trí lại trang chủ theo mockup ✅
- Layout 7:3 tham chiếu từ ảnh mockup
- Cột trái (7/10): Tin nổi bật ảnh overlay + 3 tin nhỏ grid
- Cột phải (3/10): 3 banner tuyên truyền (đỏ/xanh/xanh lá) + Tổng đài 1022

---

## 3. DECISIONS LOG

| Quyết định | Lý do | Người chốt | Không đổi? |
|------------|-------|-------------|------------|
| Layout 7:3 cho tin tức trang chủ | Theo mockup tham chiếu, cân đối nội dung + tiện ích | User | Có |
| `prisma generate` trong build script | Vercel cần generate Prisma Client trước khi compile TS | Agent | Có |
| Prisma trả null khi chưa có DB | Cho phép chạy demo mode (JSON tĩnh) mà không cần DB | Agent | Không (đổi khi có DB) |
| Banner tuyên truyền hardcode | Chưa có CMS, sau chuyển sang dynamic | Agent | Không |
| Tổng đài 1022 cột phải | Theo mockup, tạo điểm nhấn hỗ trợ người dân | User | Có |

---

## 4. OPEN ISSUES

| Vấn đề | Mức độ | Ghi chú |
|--------|--------|---------|
| Vercel deploy chưa xác nhận thành công | ⚠️ Cao | Đã push code, cần kiểm tra trên Vercel dashboard |
| `GEMINI_API_KEY` chưa cấu hình | ⚠️ Cao | Chatbot cần key thực từ Google AI Studio |
| Banner tuyên truyền chưa carousel | Thấp | Hiện hiển thị tĩnh 3 banners, chuyển carousel sau |
| Nút ← → tin tức chưa hoạt động | Thấp | Placeholder, chờ có thêm tin mới |
| PDF ~66MB trên Vercel | Trung bình | Nếu vượt 100MB output, cần Git LFS hoặc host ngoài |

---

## 5. NEXT ACTIONS

| TT | Action | Output | Ghi chú |
|----|--------|--------|---------|
| 1 | Xác nhận deploy Vercel thành công | URL demo cho khách | Kiểm tra Vercel dashboard |
| 2 | Cấu hình `GEMINI_API_KEY` | Chatbot AI hoạt động | Google AI Studio |
| 3 | Commit & push layout tin tức 7:3 | Deploy tự động | Chưa push bản mới nhất |
| 4 | Carousel hóa banner tuyên truyền | UX tốt hơn trên cột phải | Client component |
| 5 | Database migration Supabase | Thay thế JSON files | Ưu tiên cao |
| 6 | CMS Admin CRUD | Dashboard quản trị | Đã có layout cơ bản |

---

## 6. PROGRESS REPORT

| Module | Trạng thái | Tiến độ |
|--------|-----------|---------|
| Trang chủ | ✅ Hoàn thành | 100% |
| Thủ tục HC | ✅ Hoàn thành | 95% (search chưa filter) |
| Chatbot AI | ✅ Hoàn thành | 90% (cần API key) |
| Tin tức | ✅ Nâng cấp | 100% (tabs + search + 7:3 layout) |
| Giới thiệu & Liên hệ | ✅ Hoàn thành | 100% |
| Tra cứu & Đánh giá | ✅ Hoàn thành | 100% |
| Liên kết website | ✅ Hoàn thành | 100% |
| Deploy Vercel | 🔄 Đang chờ | 80% (code đã push, chờ xác nhận) |
| CMS Admin | 🔄 Cơ bản | 30% (layout + auth) |
| Database | ⏳ Chưa bắt đầu | 0% |

**Tổng tiến độ ước tính: ~78%**

---

## 7. CONTEXT ESSENTIALS

### Constraints
- **TailwindCSS v4** — dùng `@theme` thay `tailwind.config`
- **Next.js 16** — `await params` cho dynamic routes
- **Turbopack** — không dùng `require()` cho JSON, dùng `fs.readFileSync`
- **Prisma v7** — dùng `@prisma/adapter-pg`, `driverAdapters` giờ là mặc định (không cần preview feature)
- **Breakpoint nav**: `--breakpoint-nav: 1100px` — custom cho header

### Quy ước
- Data files: `data/*.json` (procedures, news, useful-links)
- Assets local: `public/images/`, `public/pdf/`
- Emoji thay FontAwesome — không phụ thuộc CDN
- Server Components mặc định — `"use client"` chỉ khi cần
- `prisma generate` tự chạy khi build (`package.json`)

### Rủi ro
- Vercel build output > 100MB → cần xử lý PDF
- Chatbot trả lời sai thủ tục → disclaimer + fallback liên hệ nhân viên
- Banner tuyên truyền hardcode → cần chuyển sang CMS

---

## 8. REFERENCES

| Tài liệu | Đường dẫn |
|-----------|-----------|
| PRD | [[docs/sml/foundation/SML_PRD]] |
| Handover trước | [[docs/handover/HANDOVER_20260508_002]] |
| Nghiên cứu tin tức | Phân tích hochiminhcity.gov.vn + ubmttq.hochiminhcity.gov.vn |
| Mockup tham chiếu | `image copy.png` (root) |
| GitHub repo | https://github.com/NghiaNguyen090499/web-ttpvhcc-xuanhoa |
| Commits phiên này | `bf01d2a` (code chính) → `9ac853d` (fix Prisma) → `d662658` (fix gitignore) |
